# assets 目录

把背景素材放在这里：

- `bg.mp4` —— 背景视频（1920×1080 推荐，时长 ≥ 8s）。
  `index.html` 已引用 `assets/bg.mp4`；**缺失时舞台的渐变背景会自动兜底**，示例仍可正常出片。
- 可选：`bg.jpg` 背景图（取消 `index.html` 中对应 `<img>` 注释即可启用）。
- 可选：`music.mp3` 背景音乐（按 README 中说明在 `#stage` 内加 `<audio>` 元素）。

无需提交二进制素材到仓库；此目录用于本地预览 / 渲染。

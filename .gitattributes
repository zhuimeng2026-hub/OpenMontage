# Cross-language fixtures MUST use LF. autocrlf on Windows would alter bytes
# and break SHA parity between Python/Go/TS contract tests.
fixtures/remix-v2/*.json  -text eol=lf
schemas/remix/*.json      -text eol=lf
# Session handoff — 2026-09-10

> Read this FIRST. Written by the previous session to skip re-discovery.

## TL;DR for the next session

Two parallel handoff threads, **do not conflate them**:

### Thread A — Working-tree changes (4 items, all green)

```
M mcp_server.py                       # + openmontage.gen_detail logger
M tools/video/minimax_video.py        # DEBUG log + submit_response_incomplete defensive branch
D generated_image_1.jpg               # one-off render artifact, delete is intentional
?? docs/xhs-via-mcp-bridge.md         # MediaCrawler → OM bridge doc (complete)
?? tools/export_render_bundle.py      # Remotion 4.0.508 self-contained bundle exporter (complete)
```

- All 4 are coherent (observability + project delivery).
- `make test-contracts` was run; **3 pre-existing failures unrelated to this PR** (kling schema_snapshot fixture missing, character-animation handoff, video_compose expects `edit_decisions` in error but new code reports `X-VClaw-User-Id`). Mention them in the PR description so reviewer doesn't blame these changes.
- `tests/tools/test_export_bundle.py` covers `tools/publishers/export_bundle.py` (publisher). The new `tools/export_render_bundle.py` has **zero tests** — per `docs/PR_REVIEW_GUIDE.md` minimum coverage, write `tests/tools/test_export_render_bundle.py` before commit.
- Memory note: `gen_detail.log` exists but is empty until `mcp_server.py` is restarted with the new handler. The handler setup in mcp_server.py runs BEFORE tool imports (verified).

### Thread B — `xiaohongshu-neon-night-30s` (paused by user choice)

Project: **小鹏哥 · 霓虹潮牌单品亮相 30s**, pipeline=`cinematic`, runtime=Remotion, composition=templated, provider=`MiniMax-Hailuo-2.3-Fast`.

- **Target 30s, delivered 17.625s** in `renders/partial_18s_v1.mp4` (768×1364, h264+aac, 5.14 MB).
- **3 of 5 sections completed**: clip_1_hook, clip_2_escalation, clip_3_reveal (each 5.875s).
- **2 sections BLOCKED by quota 2067**: clip_4_detail (extreme close-up), clip_5_landing (brand reveal).
- **User decision (2026-09-10T07:08:00)**: "现在停，后期重试（等 quota 重置）". Per AGENT_GUIDE "No Unilateral Substitutions", production is **paused on user direction**. Do not resume without explicit user go-ahead.
- Quota observed this run: **4 successful generations** (sample + 3 production). Memory currently says "≈3"; pending update says revise to ~4.

**Resume checklist (already on disk, no re-gen needed)**:

- ✅ `assets/images/first_frame_4_detail.png` + `first_frame_5_landing.png` exist
- ✅ `checkpoint_edit.json` `human_approved=true` (5 cuts + 3 title cards + 1 brand overlay)
- ✅ `assets/decision_log.json` has 8 decisions recorded
- ✅ OM wire-up changes landed in commit `7814ed8`: `tools/video/minimax_video.py +image_urls`, `tools/video/minimax_video_direct.py +image_local_path`

**4 paths forward (from `progress_log.json` `pending_stages`)**:

| Path | Trigger | Work |
|---|---|---|
| A. Wait for quota reset, finish 4-5 | User purchases credits OR cycle resets | re-run minimax_video_direct ×2 → concat 5 → compose → publish |
| B. Buy quota now, finish 4-5 | User buys credit pack | same as A, available immediately |
| C. Accept 18s as final | User accepts 18s | update `progress_log.project_state.intended_total_duration_seconds=18`, trim `edit_decisions` to 3 cuts, re-render |
| D. Pad 17.625s → 30s with text_card/stock | User refuses more video quota | ffmpeg concat + 12s title-card animation |

User picked A last; ask before picking again.

## Frozen verdicts (do not re-litigate)

- `render_runtime` = Remotion. Already locked at proposal; do not swap to HyperFrames.
- `composition_mode` = templated (NOT atelier). Already locked at proposal; user picked speed over hero-craft.
- `video_provider` = minimax_video_direct via MiniMax-Hailuo-2.3-Fast. Do not substitute (memory: Hailuo safety softening violence — irrelevant here; memory: quota-2067 — relevant).
- `music_provider` = Pixabay CC0 "Brain Implant" by VasilYatsevich, already downloaded.

## Memory updates still pending (do these first when convenient)

1. Update `minimax-hailuo-2-3-quota-2067.md`: change "≈3" → "~4 successful per Token Plan cycle" (this run did 4 before 2067).
2. Update `minimax-multimodal-newapi-verification.md`: add note that `minimax_image` now surfaces `image_urls` (OSS signed URLs) — chain directly to `minimax_video_direct` I2V without external image host (eliminates imgbb dependency).

## Key file paths

- `projects/xiaohongshu-neon-night-30s/HANDOFF-FROM-PREV-SESSION.md` — a previous session's notes if present (read it)
- `projects/xiaohongshu-neon-night-30s/checkpoint_*.json` — gates, status
- `projects/xiaohongshu-neon-night-30s/artifacts/{progress_log,decision_log,edit_decisions,script,scene_plan}.json`
- `projects/xiaohongshu-neon-night-30s/events.jsonl` — full event timeline (append-only)
- `projects/xiaohongshu-neon-night-30s/renders/partial_18s_v1.mp4` — the 17.625s partial deliverable
- `/opt/OpenMontage_Voicebox/MEMORY.md` (user auto-memory) — frozen verdicts, project quirks

## Do not

- Do not resume the `xiaohongshu-neon-night-30s` run unilaterally. Ask first.
- Do not commit the working-tree changes before deciding whether to add `tests/tools/test_export_render_bundle.py` first (next-session owner decides).
- Do not silently rewrite the user-decision log; append new entries with the same `(category, subject)` pair.
# T01 contract test - REMIX v2 schema and frame conversion.
# Tests the Python validator against the shared fixture set.
# Cross-language parity is verified by T01 consistency check (SHA + run_all_fixtures parity).
# Upstream OpenMontage MCP endpoint. The path must be included.
UPSTREAM_MCP_URL=http://lanes.ymxt.top:8900/mcp
# Upstream token (legacy mcp_key is also accepted).
UPSTREAM_MCP_TOKEN=
mcp_key=
# Required second token for clients calling this public proxy.
PROXY_CLIENT_TOKEN=
PORT=8080
